# Task Spec Template (for async agents)

Use this when handing work to a background agent. It front-loads context so the agent can run longer without supervision.

## Context

- Goal:
- Non-goals:
- Constraints (frozen files, copy rules, design rules):

## Target files

- Read first:
- Allowed to edit:
- Must not touch:

## Acceptance criteria

- Functional:
- Visual:
- Performance (if relevant):

## Verification

- Run: `PLAN_ID=<id> pnpm verify`
- Attach proof:
  - screenshots path
  - (optional) `pnpm self-review` output

